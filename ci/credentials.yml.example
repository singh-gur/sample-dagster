---
# Concourse Credentials Template
# Copy this file to credentials.yml and fill in your actual values
# DO NOT commit credentials.yml to version control

# GitHub credentials (HTTPS)
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
github_username: your-github-username
github_token: ghp_your_personal_access_token_here

# Docker registry credentials
registry_username: your-registry-username
registry_password: your-registry-password

# Cosign image signing credentials
# Generate keys with: cosign generate-key-pair
# This will create cosign.key (private) and cosign.pub (public)
cosign_private_key: |
  -----BEGIN ENCRYPTED COSIGN PRIVATE KEY-----
  YOUR_COSIGN_PRIVATE_KEY_HERE
  -----END ENCRYPTED COSIGN PRIVATE KEY-----

cosign_public_key: |
  -----BEGIN PUBLIC KEY-----
  YOUR_COSIGN_PUBLIC_KEY_HERE
  -----END PUBLIC KEY-----

# Password used when generating the cosign key pair
cosign_password: your-cosign-key-password
